{
  "_README": "Copy this file to secrets.json (gitignored) and fill in real values. VS Code F5 picks them up via --dart-define-from-file in .vscode/launch.json. The trading API key is no longer needed here — it lives server-side in the Supabase Edge Function (set via `supabase secrets set TRADING_API_KEY=...`).",
  "SUPABASE_URL": "https://your-project-ref.supabase.co",
  "SUPABASE_ANON_KEY": "your-supabase-anon-key-here"
}

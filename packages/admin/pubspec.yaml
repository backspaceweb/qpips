name: qp_admin
description: QuantumPips admin panel — web-only app for the platform operator.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.0.5
  supabase_flutter: ^2.12.4
  qp_core:
    path: ../core
  qp_design:
    path: ../design

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

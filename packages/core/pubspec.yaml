name: qp_core
description: QuantumPips core — domain models, repositories, Chopper API client, interceptors.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  chopper: ^6.1.4
  json_annotation: ^4.8.0
  provider: ^6.0.5
  logging: ^1.1.1
  supabase_flutter: ^2.12.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.3.3
  chopper_generator: ^6.0.0
  json_serializable: ^6.6.1
  swagger_dart_code_generator: ^2.10.1

flutter:
